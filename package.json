{
  "name": "forgeon-hono-bun-playground",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun dist/index.js",
    "dev": "bun run --hot src/index.ts"
  },
  "dependencies": {
    "hono": "^4.8.9"
  },
  "devDependencies": {
    "@types/bun": "^1.3.1",
    "typescript": "^5.0.0"
  },
  "engines": {
    "bun": "1.1.0"
  }
}
